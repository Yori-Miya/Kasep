{
  "name": "kasep-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "KASEP Frontend - Vanilla HTML, CSS, and JavaScript with Tailwind CSS",
  "scripts": {
    "build": "node build-css.js",
    "dev": "npm run build",
    "watch": "node build-css.js --watch",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:ci": "jest --ci --coverage --maxWorkers=2"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "@babel/core": "^7.28.6",
    "@babel/preset-env": "^7.28.6",
    "@testing-library/dom": "^9.3.4",
    "@testing-library/user-event": "^14.5.1",
    "babel-jest": "^30.2.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "tailwindcss": "^4.1.11"
  }
}
